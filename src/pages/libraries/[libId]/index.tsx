// Support for default locale without path prefix
import {LibraryPage, getServerSideProps} from '../../[locale]/libraries/[libId]';

export {LibraryPage, getServerSideProps};

export default LibraryPage;
