// Support for default locale without path prefix
import {ComponentPage, getServerSideProps} from '../../[locale]/components/[libId]/[componentId]';

export {ComponentPage, getServerSideProps};

export default ComponentPage;
