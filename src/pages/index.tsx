import {Landing} from '../components/Landing/Landing';
import {Layout} from '../components/Layout/Layout';

const Home = () => {
    return (
        <Layout>
            <Landing />
        </Layout>
    );
};

export default Home;
