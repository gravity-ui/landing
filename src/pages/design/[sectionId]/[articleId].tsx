// Support for default locale without path prefix
import {ArticlePage, getServerSideProps} from '../../[locale]/design/[sectionId]/[articleId]';

export {ArticlePage, getServerSideProps};

export default ArticlePage;
