# @gravity-ui/tsconfig

## インストール
```
npm install --save-dev @gravity-ui/tsconfig
```

## 使用方法
プロジェクトのルートに `tsconfig.json` ファイルを作成し、以下の内容を追加してください。

```json
{
    "extends": "@gravity-ui/tsconfig/tsconfig.json"
}
```