# @gravity-ui/tsconfig

## Instalar
```
npm install --save-dev @gravity-ui/tsconfig
```

## Usar
Adicione um arquivo `tsconfig.json` na raiz do projeto com o seguinte conteúdo:

```json
{
    "extends": "@gravity-ui/tsconfig/tsconfig.json"
}
```