Используется, когда нужно выбрать один из нескольких взаимоисключающих вариантов.

### Когда стоит применять

Как правило, `Radio`, как и чекбокс, не запускает действие мгновенно. Запуск действия происходит по нажатии кнопки подтверждения («Сохранить» / «Применить» / «Отправить»).

Пояснительный текст лейбла должен отвечать на вопрос «Как?» или «Какой?».

**Когда не использовать**

Не рекомендуется применять как элемент переключения содержимого страницы или меню. Таким функционалом обладают компоненты [RadioButton](https://gravity-ui.com/design/guides/radio-button) и `Tabs`.

### Структура

Состоит из нескольких компонентов `Radio`, имеет 1 — Radio-элемент и 2 — уточняющий лейбл.

![Structure](/static/images/design/RadioGroup/Structure.png)

Radio всегда применяется вместе с лейблом, поясняющим его значение.

По умолчанию всегда выбран первый пункт.

Опционально можно оставлять переключатели в невыбранном состоянии (когда все варианты очищены).

После выбора элемента в группе пользователь не может отменить выбор, чтобы восстановить исходное состояние группы (без обновления страницы).

При использовании компонента рекомендуем всегда выбирать первый вариант в списке.

### Размеры

На странице доступны два размера: `M` и `L`. Выбор размера компонента зависит от размера остальных элементов управления на странице. Если на странице используются размеры `L` для инпутов, кнопок и других элементов, то рекомендуется использовать соответствующий размер радио-кнопок. Размер шрифта на странице также зависит от выбранного размера контролов.

![Sizes](/static/images/design/RadioGroup/Sizes.png)

### Расположение

Может применяться в горизонтальном и вертикальном положении.

![Positioning](/static/images/design/RadioGroup/Positioning.png)
