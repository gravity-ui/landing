Se utiliza cuando tienes que elegir entre varias opciones mutuamente excluyentes.

### Cuándo usar

Similar a los checkboxes, los Radio no activan una acción inmediatamente. Para hacerlo, debes hacer clic en el botón de confirmación (Guardar / Aplicar / Enviar).

El texto explicativo en la etiqueta debe responder a la pregunta: "¿Cómo?" o "¿Cuál?".

**Cuándo no usar**

No uses Radio para cambiar entre contenido de página o menú. Esta funcionalidad está soportada por los componentes [radio-button](https://gravity-ui.com/design/guides/radio-button) y tab.

### Estructura

Contiene múltiples componentes Radio, con 1 — elemento Radio, 2 — etiqueta explicativa.

![Estructura](/static/images/design/RadioGroup/Structure.png)

Radio siempre se usa con una etiqueta que explica su significado.

La primera opción se selecciona por defecto.

También puedes dejar los controles de alternancia sin seleccionar (cuando se eliminan todas las opciones).

Una vez que se ha seleccionado un elemento dentro de un grupo, el usuario no puede deseleccionarlo para devolver el grupo a su estado original (sin actualizar la página).

Al usar un componente, te recomendamos que siempre selecciones la primera opción de la lista.

### Tamaños

La página ofrece dos tamaños: M y L. El tamaño de los otros controles de la página determina la elección del tamaño de este componente. Si la página utiliza el tamaño L para inputs, botones y otros elementos, recomendamos usar el tamaño correspondiente para los botones de radio. El tamaño seleccionado para los controles también determina el tamaño de la fuente en la página.

![Tamaños](/static/images/design/RadioGroup/Sizes.png)

### Posicionamiento

Puedes usar este elemento tanto en diseño horizontal como vertical.

![Posicionamiento](/static/images/design/RadioGroup/Positioning.png)