El radio de las esquinas de los controles se puede anular. Para ello, utilice las siguientes variables:

- --g-border-radius-xs: 3 px;
- --g-border-radius-s: 5 px;
- --g-border-radius-m: 6 px;
- --g-border-radius-l: 8 px;
- --g-border-radius-xl: 10 px;
- --g-border-radius-2xl: 16 px;

![Corner radius](/static/images/design/CornerRadius/1.png)

Puede anular los valores de Variables. ****

Las variables se utilizan en los controles de los tamaños correspondientes. Si cambias el valor de la variable, el radio de las esquinas de todos los controles del mismo tamaño cambiará en consecuencia. Los controles afectados por esta variable aparecen en la página de personalización de marca de la biblioteca de Figma. Dado que los distintos controles tienen diferentes rangos de tamaño, es posible que los conjuntos de controles no coincidan. Las etiquetas, por ejemplo, no vienen en tamaños superiores a L, por lo que las variables que comienzan por --g-border-radius-l no se utilizan en este componente y no afectan a su apariencia.

![Corner radius](/static/images/design/CornerRadius/2.png)

El radio de la esquina del elemento anidado del botón de verificación viene determinado por las fichas y se calcula mediante la fórmula «valor de la ficha: 3» (donde 3 es la distancia entre los elementos internos y externos). En la versión actual, debe realizar los cálculos manualmente e introducir los resultados en los siguientes símbolos:

![Corner radius](/static/images/design/CornerRadius/3.png)

![Corner radius](/static/images/design/CornerRadius/4.png)